pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'make -C calnex clean all'
                archiveArtifacts artifacts: '**/build/*.tar', fingerprint: false
            }
        }
    }
}